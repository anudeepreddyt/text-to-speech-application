
### One correction

README lo **Speech History API ippudu create cheyyaledu**, kabatti `GET /api/speech-history` ni **currently implemented API list lo pettakunda**, future feature ga pettadam cleaner.

So currently:

```markdown
## Backend APIs

| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/health` | Check backend status |
| POST | `/api/tts` | Generate speech |
| GET | `/api/voices` | Get available voices |
| GET | `/api/languages` | Get supported languages |